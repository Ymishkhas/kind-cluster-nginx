apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: private-gateway
  namespace: istio-ingress
spec:
  selector:
    istio: ingress
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - grafana.localhost
        - prom.localhost